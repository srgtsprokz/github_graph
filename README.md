# Автокоммиты по датам

## 📌 Описание

Скрипт автоматически генерирует последовательность дат и записывает их в файл для последующего создания git-коммитов с нужными временными метками.

## ⚙️ Технологии

- Python 3.9.13

## 🚀 Запуск

1. Убедитесь, что установлен Python 3.9.13
2. Запустите скрипт:
   ```
   python main.py
   ```
3. В `data.txt` появится список дат, который можно использовать для автоматических коммитов.

## 📤 Git push (по желанию)

Вы можете самостоятельно добавить свой код.

Например:

```
git push -u origin master
```

или

```
git push -u origin main
```

Выберите нужную ветку или измените команду под свой репозиторий.
